// Form Data -> UserName -> password -> servside ->

let user = {
  userName: "Chitty",
  age: 23,
  location: "TN",
};

// Key value pair in for ( in )
// array is for ( in )

for (let edata in user) {
  console.log(user[edata]);
}

// 13. Write a program to read 5 numbers and print only the even numbers

const num = [1, 52, 93, 21, 54]
let i=indexOf(0);
while ((num(indexOf))%2==0) {
    console.log(num(indexOf));
    indexOf++;
}